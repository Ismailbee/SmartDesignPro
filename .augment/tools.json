{
  "tools": {
    "playwright": {
      "type": "command",
      "command": "npx",
      "args": ["playwright", "test", "--reporter=json", "--reporter=html"],
      "description": "Run Playwright tests for responsiveness and functionality checks across multiple viewports (mobile, tablet, desktop).",
      "cwd": "."
    },
    "lighthouse": {
      "type": "command",
      "command": "npx",
      "args": ["lighthouse", "http://localhost:5174", "--output=json", "--output-path=./reports/lighthouse-report.json", "--quiet"],
      "description": "Run Lighthouse performance, accessibility, and SEO audits on the application.",
      "cwd": "."
    }
  }
}

